<p id="notice"><%= notice %></p>

<html lang="pt-br">
<div id="main-container" class="container" style="text-align: left">
  <div class="row">

    <div class="Conteudo">
      <h1 class="Fonte">Áreas</h1>

      <table class="TabelaAreas">


        <tbody>

        <% @areas.each do |area| %>
            <tr>
              <td><%= link_to area.descricao, area_unidades_path(area.id),  class:'botaoPadrao' %></td>
            </tr>
        <% end %>
        </tbody>
      </table>

    </div>
  </div>
</div>

<%= will_paginate @areas %>