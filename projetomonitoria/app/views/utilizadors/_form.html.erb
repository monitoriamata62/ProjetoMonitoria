<%= form_for(utilizador) do |f| %>
  <% if utilizador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(utilizador.errors.count, "error") %> prohibited this utilizador from being saved:</h2>

      <ul>
      <% utilizador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :matricula %>
    <%= f.text_field :matricula %>
  </div>

  <div class="field">
    <%= f.label :identificador %>
    <%= f.number_field :identificador %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
